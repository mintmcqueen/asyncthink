# Dependencies
node_modules/

# Build output (root-only — server/dist is committed for plugin distribution)
/dist/

# Environment files (contains secrets)
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Test coverage
coverage/

# Debug logs
*.log
npm-debug.log*

# TypeScript cache
*.tsbuildinfo

# Research scratch (R2-D ruling: dev/research is personal scratch, not committed)
dev/research/

# Optional Aikido IOC enrichment — local-only, populated by:
#   npm run audit:supply-chain:refresh -- --with-aikido
# License of Aikido feed data is not separately published; we don't redistribute.
server/scripts/ioc/aikido-enrichment.json
