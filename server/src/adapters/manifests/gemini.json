{
  "id": "gemini",
  "displayName": "Gemini CLI",
  "binary": "gemini",
  "defaultModel": "gemini-2.5-flash",
  "requiredEnv": ["GEMINI_API_KEY", "GOOGLE_API_KEY"],
  "defaultTimeoutMs": 60000,
  "description": "Google Gemini CLI in --approval-mode plan (read-only navigation) for fast metacognitive research with native session resume."
}
