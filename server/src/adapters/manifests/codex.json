{
  "id": "codex",
  "displayName": "Codex CLI",
  "binary": "codex",
  "defaultModel": "gpt-5.4",
  "requiredEnv": ["OPENAI_API_KEY"],
  "defaultTimeoutMs": 120000,
  "description": "OpenAI Codex CLI via 'codex exec --sandbox read-only --ask-for-approval never' for code-aware research with native session resume."
}
